name: CI

on:
  pull_request:
    branches: [main]

jobs:
  test:
    name: Core Tests
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4

      - name: Run Core tests
        run: swift test --package-path Core
