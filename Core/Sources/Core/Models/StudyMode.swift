public enum StudyMode: String, Codable, CaseIterable, Sendable {
    case reading = "reading"
    case listeningWithText = "listening_with_text"
    case listeningWithoutText = "listening_without_text"
}
