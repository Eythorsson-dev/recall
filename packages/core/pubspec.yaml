name: core
description: Shared business logic for Recall — models, repositories, FSRS scheduler.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.5

dependencies:
  drift: ^2.27.0
  fsrs: ^2.0.1

dev_dependencies:
  drift_dev: ^2.27.0
  build_runner: ^2.4.15
  test: ^1.25.15
  sqlite3: ^2.7.4
  lints: ^6.1.0
